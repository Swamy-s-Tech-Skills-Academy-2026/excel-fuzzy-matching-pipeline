# excel-fuzzy-matching-pipeline
End-to-end Excel automation pipeline for deterministic fuzzy matching between descriptions and amounts. Handles numeric-sensitive matching using custom scoring logic, generates auditable Code–Amount mappings, and produces explainable outputs with detailed matching logs.
